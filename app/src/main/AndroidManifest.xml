<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javirock.cleanarchevents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentationlayer.ui.activities.StartActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentationlayer.ui.activities.RegistrationActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".presentationlayer.ui.activities.UserActivity"
            android:theme="@style/AppTheme.Dark"></activity>
    </application>

</manifest>